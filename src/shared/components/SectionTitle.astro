---
interface Props {
  title: string;
  subtitle?: string;
  centered?: boolean;
}

const { title, subtitle, centered = true } = Astro.props;
---

<div class={`mb-16 ${centered ? 'text-center' : ''}`} data-aos="fade-up">
  <h2 class="text-3xl md:text-4xl font-serif font-bold text-primary mb-6 relative inline-block">
    {title}
    <span class="absolute -bottom-2 left-0 w-full h-1 bg-accent/30 rounded-full"></span>
  </h2>
  {subtitle && (
    <p class="text-muted text-lg max-w-2xl mx-auto leading-relaxed">
      {subtitle}
    </p>
  )}
</div>